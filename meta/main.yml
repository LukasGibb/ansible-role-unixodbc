galaxy_info:
  role_name: unixodbc
  author: LukasGibb
  description: An Ansible role that installs and configures UnixODBC
  company: cloudjourneyman.com

  license: MIT

  min_ansible_version: 1.2

  platforms:
  - name: Ubuntu
    versions:
    - bionic

  galaxy_tags:
    - unixodbc
    - odbc
    - mysql
    - database

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
